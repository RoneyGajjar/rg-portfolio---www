export const Section = (props: any) => {
  return (
    <div className="mx-auto max-w-7xl p-4">
        {props.children}
    </div>
  )
}

export const Button = (props: any) => {
    return (
        <button>BTN</button>
    )
}